#include<bits/stdc++.h>
using namespace std;
class Node
{
    public:
        int val;
        Node * next;
    Node(int val)
    {
        this->val=val;
        this->next=next;
    }
};
void insert(Node * head,int val)
{
    Node * newNode = new Node(val);
}
int main ()
{
     Node * head = NULL;
     cout<<"Option 1: Insert at tail"<<endl;
     int op;
     cin>>op;
     if(op==1)
     {

     }
    return 0;
}